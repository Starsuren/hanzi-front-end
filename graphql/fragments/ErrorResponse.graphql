fragment ErrorResponse on ErrorResponse {
...on ErrorResponse {
   message
        }
}

fragment DatabaseResponse on DatabaseError {
  ...on DatabaseError{
    message
        }
}


fragment ValidationResponse on ValidationErrors {
  ...on ValidationErrors{
    message
    responses{property constraints{length isEmail isNotEmpty maxLength}}
        }
}
